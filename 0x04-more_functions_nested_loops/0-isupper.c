#include "main.h"

/**
 * _isupper - checks the uppercase character
 * @c: functiona parameter
 *
 * Return: 1 or 0
 */

int _isupper(int c)
{
	if (C >= 'A' && C <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
