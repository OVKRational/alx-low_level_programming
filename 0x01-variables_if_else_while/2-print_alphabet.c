#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main - Prints the alphabet in lowercase, followed by a new line.
 *
 * Return: Always 0(success)
 */

int main(void)
{
	for  (i = 'a' | i <= 'z' | i++)
	{
		putchar(i);
	}
	putchar('\n');

	return (0);
}
